apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: backend
  endpoints:
  - port: http-metrics
    path: /metrics
